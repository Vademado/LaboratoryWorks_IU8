#pragma once
#include <thread>
#include <iostream>
#include <chrono>

void fibonacci(unsigned, unsigned i = 2, unsigned f1 = 1, unsigned f2 = 1);

void natural_numbers(unsigned);

void random_numbers(unsigned);